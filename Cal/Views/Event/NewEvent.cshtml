@model Event
@{
    ViewData["Title"] = "Новое событие.";
}

@if(Model is not null)
{
    <h1>Новое событие на @Model.Date.Date.ToShortDateString()</h1>
}

@*<script>
    function textLength(value){
       var maxLength = 200;
       if(value.length > maxLength) return false;
       return true;
    }

    document.getElementById('text').onkeyup = function(){
         document.getElementById('name-character-count').textContent = this.value;
         if(!textLength(this.value)) alert('text is too long!');
    }
</script>*@

<div class="row">
    <div class="col-md-6 offset-3">
        @if (TempData["Error"] != null)
        {
            <div class="col-md-12 alert alert-danger">
                <span><b>Извините!</b> - @TempData["Error"] </span>
            </div>
        }

        <div class="row">
            <div class="col-md-8 offset-2">
                <form asp-action="NewEvent">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div class="form-group">
                        <label asp-for="Name" class="control-label"></label>
                        <input asp-for="Name" class="form-control" placeholder="Введите название события..." maxlength="200" id="text" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                        <div class="text-muted">Осталось символов: <span id="name-character-count">200</span></div>
                    </div>

                    <div class="form-group">
                        <label asp-for="Description" class="control-label"></label>
                        <textarea asp-for="Description" class="form-control" placeholder="Опишите ваши планы в подробностях..." maxlength="3000"></textarea>
                        <span asp-validation-for="Description" class="text-danger"></span>
                        <div class="text-muted">Осталось символов: <span id="description-character-count">3000</span></div>
                    </div>
                    <div class="form-group">
                        <label asp-for="Date" class="control-label"></label>
                        <input asp-for="Date" class="form-control" />
                        <span asp-validation-for="Date" class="text-danger"></span>       
                    </div>
                    <div class="form-group">
                        <input class="btn btn-outline-success float-right" type="submit" value="Добавить событие" />
                        <a class="btn btn-outline-secondary" asp-controller="Home" asp-action="Index">Отмена</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>